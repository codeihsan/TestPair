var handleError = require('../../lib/handle-error');

handleError(function() {}, function(a, b, c) {
    if (a !== 'a') throw 'Error';
    if (b !== 'b') throw 'Error';
    if (c !== 'c') throw 'Error';
})(null, 'a', 'b', 'c');
